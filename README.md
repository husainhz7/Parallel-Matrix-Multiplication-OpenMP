# Parallel implementation of matrix multiplication and Prefix Scan using OpenMP

Using Hillis Steele Algorithm for Prefix Scan

compile using: 

```    gcc -fopenmp  parallel_matrix.c  ```

```    gcc -fopenmp  parallel_prefixsum.c -lm  ```